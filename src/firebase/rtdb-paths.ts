
/**
 * A centralized place for all Realtime Database root paths.
 * This makes it easy to change the root node if the database structure changes
 * (e.g., from 'data' to 'prod_data').
 */
export const DB_ROOT_PATH = 'data';
